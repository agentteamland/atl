# core/

Global rules + skills for the ATL platform (the v1 `agentteamland/core` content).

**Status:** placeholder. Ported in a later v2 step (decision doc, migration sequencing step 3) — rewritten to the simplified concept set (unified agent-KB, no `memory`, shrunk docs-sync).
