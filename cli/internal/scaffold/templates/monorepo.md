# {{NAME}} — contributor orientation

<!-- Lean monorepo orientation (soft budget ~30 lines): point at things, don't inline
     them. Architecture decisions + the knowledge map live in their own files — link,
     don't copy. Volatile / sprint state belongs in a tracker, not here. -->

## Layout
| Path | What |
|------|------|
| `<dir>/` | <what lives here> |

## Conventions
- <Commit format, the CI gate, how releases are cut.>
