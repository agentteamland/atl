# Global Claude Code defaults — <your name>

<!-- This file loads into EVERY project, so keep it lean (soft budget ~80 lines) —
     every line costs context each session. Put only universal, project-independent
     defaults here; a project's own CLAUDE.md wins on conflict. ATL manages nothing
     in this file — it is yours. -->

## Working style
- <How should Claude communicate with you? Language, tone, how to present options.>

## Engineering defaults
- <The discipline you want in every project: e.g. the simplest change that works,
  surgical edits that touch only what the task needs, fix the root cause, and prove
  it works before calling it done.>

## Golden rule
- <Your one-liner — the principle you most want Claude to hold to.>
