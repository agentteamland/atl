# {{NAME}}

<!-- A project starter from `atl init` (soft budget ~60 lines). The sections below
     are YOURS — fill them from how the code actually behaves; ATL never overwrites
     content outside its managed marker blocks. The /brainstorm and /drain skills
     maintain their own marker blocks (active brainstorms, knowledge index) near the
     top of this file automatically — leave room for them. -->

## Stack
<!-- Languages, frameworks, datastores, key services. Capture how it behaves; don't invent business rules. -->

## Commands
<!-- The exact commands a new contributor needs: build / test / run / lint. -->

## Conventions
<!-- Naming, layout, commit/PR rules. Align meta-architecture, not syntax. -->

## Skill routing (optional)
<!-- Up to ~5 rows that complement skill-selection-discipline. Delete this section if unused.
| When you touch…    | Use            |
|--------------------|----------------|
| `**/*.sql`         | /db-review     |
-->

## State
<!-- Keep this file stable: put sprint / volatile status in a tracker, not here. -->
