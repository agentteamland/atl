# docs/

The VitePress docs site (EN canonical → TR mirror), served at the platform docs URL.

**Status:** placeholder. Ported in a later v2 step (decision doc, migration sequencing step 3). The cross-repo docs-sync machinery is retired under v2 (concept inventory item 12); only the EN→TR site flow survives.
